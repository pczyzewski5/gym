{{ form_start(form) }}
<div class="field">
    <label class="label">{{ form_label(form.name) }}</label>
    <div class="control">
        {{ form_widget(form.name) }}
    </div>
    <p class="help is-danger">{{ form_errors(form.name) }}</p>
</div>

<div class="field">
    <label class="label">{{ form_label(form.tags) }}</label>
    <div class="control">
        {{ form_widget(form.tags) }}
    </div>
    <p class="help is-danger">{{ form_errors(form.tags) }}</p>
</div>

<div class="field">
    <label class="label">{{ form_label(form.description) }}</label>
    <div class="control">
        {{ form_widget(form.description) }}
    </div>
    <p class="help is-danger">{{ form_errors(form.description) }}</p>
</div>

<div class="field">
    <label class="label">{{ form_label(form.image) }}</label>
    <div class="control">
        <div class="file">
            <label class="file-label">
                {{ form_widget(form.image) }}
                <span class="file-cta">
                    <span class="file-icon">
                        <i class="fas fa-upload"></i>
                    </span>
                    <span class="file-label">
                        Wybierz plik...
                    </span>
                </span>
            </label>
        </div>
    </div>
    <p class="help is-danger">{{ form_errors(form.image) }}</p>
</div>

<div class="field">
    <div class="control">
        {{ form_widget(form.zapisz) }}
    </div>
</div>
{{ form_end(form) }}

<script src="{{ asset('js/attach_tags_input.js') }}"></script>
<script src="{{ asset('js/tags_helper.js') }}"></script>