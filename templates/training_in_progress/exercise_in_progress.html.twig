{% extends 'base.html.twig' %}
{% block body %}
    <div class="container is-max-desktop">
        <p class="title is-3">{{ station['name']|capitalize }}</p>
        <p class="subtitle is-5">
            <span class="icon">
                <i class="fas fa-dumbbell"></i>
            </span>
            {{ exercise.getName()|capitalize }}
        </p>

        blah
    </div>
{% endblock %}