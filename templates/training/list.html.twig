{% extends 'base.html.twig' %}
{% block body %}
    <style>
        .table-item {
            cursor: pointer;
        }
    </style>
    <table class="table is-striped is-hoverable is-fullwidth">
        <thead>
        <tr>
            <th>#</th>
            <th>data treningu</th>
            <th>co tydzień</th>
            <th>tagi</th>
        </tr>
        </thead>
        <tbody>
        {% for key, character in trainings %}
            <tr class="table-item">
                <td>{{ key + 1 }}</td>
                <td>{14 lut pon</td>
                <td>tak</td>
                <td>tagi</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <script src="{{ asset('js/list_helper.js') }}"></script>
{% endblock %}