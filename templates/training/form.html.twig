{{ form_start(form) }}
<div class="field">
    <label class="label">{{ form_label(form.date) }}</label>
    <div class="control">
        <div class="select is-fullwidth">
            {{ form_widget(form.date) }}
        </div>
    </div>
    <p class="help is-danger">{{ form_errors(form.date) }}</p>
</div>

<div class="field">
    <label class="label">{{ form_label(form.repeated) }}</label>
    <div class="control">
        {{ form_widget(form.repeated) }}
    </div>
    <p class="help is-danger">{{ form_errors(form.repeated) }}</p>
</div>

<div class="field">
    <label class="label">{{ form_label(form.tags) }}</label>
    <div class="control">
        {{ form_widget(form.tags) }}
    </div>
    <p class="help is-danger">{{ form_errors(form.tags) }}</p>
</div>

<div class="field">
    <div class="control">
        {{ form_widget(form.zapisz) }}
    </div>
</div>
{{ form_end(form) }}

<script src="{{ asset('js/tags_helper.js') }}"></script>